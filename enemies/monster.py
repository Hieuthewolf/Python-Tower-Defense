from .enemy import Enemy

# Creates instances of the different monsters (could all be compiled as one instance but separating them just in case
# one monster could have a different attribute from the rest)

class Monster_1(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_2(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_3(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_4(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_5(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_6(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_7(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_8(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_9(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)

class Monster_10(Enemy):
    def __init__(self, name, map_label):
        super().__init__(name, map_label)




    


